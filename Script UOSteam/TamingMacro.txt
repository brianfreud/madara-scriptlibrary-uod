if not listexists 'tame'
 createlist 'tame'
 pushlist 'tame' '0xd9' // This is where you populate the list with graphic ID of MOBs you are taming
endif
for 0 to 'tame'
 if findtype tame[] 'any' 'world' 0 3
  autotargetobject 'found'
  useskill 'Animal Taming'
  pause 10000
  // Added prefix '@' to avoid system warnings
  while not @injournal 'You fail to tame'
   if @injournal 'It seems'
    rename 'found' 'ArchonWasHere'
    pause 200
    msg 'ArchonWasHere release'
    waitforgump 0xee76eb06 3000
    replygump 0xee76eb06 2
    // Stop will terminate the macro, should not you clearjournal?
    @clearjournal
    stop
   endif
  endwhile
 endif
endfor
@clearjournal